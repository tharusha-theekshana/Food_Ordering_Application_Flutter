import 'package:flutter/material.dart';

class ModifierItemWidget extends StatelessWidget {
  const ModifierItemWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
